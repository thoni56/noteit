import { Meteor } from 'meteor/meteor';
import '../imports/api/notes.js';
import '../imports/api/tags.js';
import '../imports/api/notetags.js';

Meteor.startup(() => {
  // code to run on server at startup
});
