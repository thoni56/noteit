{
  "name": "noteit",
  "private": true,
  "scripts": {
    "start": "meteor run --inspect",
    "debug": "meteor run --inspect",
    "test": "meteor test --inspect --driver-package cultofcoders:mocha --port 3010",
    "test-debug": "meteor test --inspect --driver-package cultofcoders:mocha --port 3010",
    "test-app": "TEST_WATCH=1 meteor test --full-app --driver-package cultofcoders:mocha",
    "visualize": "meteor --production --extra-packages bundle-visualizer"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.54",
    "@types/simplemde": "^1.11.7",
    "babel-runtime": "^6.26.0",
    "bcrypt": "^3.0.0",
    "eslint": "^5.6.0",
    "file-saver": "^1.3.8",
    "meteor-node-stubs": "^0.4.1",
    "papaparse": "^4.6.0",
    "simplemde": "^1.11.2"
  },
  "meteor": {
    "mainModule": {
      "client": "client/main.js",
      "server": "server/main.js"
    }
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "sinon": "^6.0.1"
  }
}
